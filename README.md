# Mirror Input Sync

Chrome extension that mirrors text between tabs.

## Install

1. Open `chrome://extensions`
2. Enable **Developer mode**
3. Click **Load unpacked** and select this folder

## Use

1. Open two tabs
2. Type in an `input` or `textarea`
3. The matching field updates in the other tab
